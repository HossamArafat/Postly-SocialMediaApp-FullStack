
const Failed = () => {
  return (
    <button className='p-2 border-2 border-gray-300 rounded cursor-pointer' onClick={() => window.location.reload()}>Failed, retry</button>
  )
}

export default Failed
